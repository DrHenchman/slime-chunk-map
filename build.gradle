apply plugin: 'java'

repositories {
    jcenter()
}

jar {
    manifest {
        attributes(
                'Main-Class': 'slimechunkmap.Main'
        )
    }
    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }
}
